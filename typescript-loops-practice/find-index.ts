/* exported findIndex */

function findIndex(array: unknown[], value: unknown): any {
  for (let i = 0; i < array.length; i++) {
    if (array[i] === value) {
      return i;
    }
  }
  return -1;
}
