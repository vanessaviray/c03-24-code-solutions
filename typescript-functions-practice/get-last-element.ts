/* exported getLastElement */
function getLastElement(array: unknown[]): any {
  const lastElementIndex: number = array.length - 1;
  const lastElement: any = array[lastElementIndex];
  return lastElement;
}
